<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- в свойстве include мы говорим какую разметку сюда подключаем
     (в нашем случае даём ссылку на card_post )
     (это делается для избежания дублирования с кодом в схеме card_post)
     -->
    <include
        android:id="@+id/postOne"
        layout="@layout/card_post"/>

</ScrollView>